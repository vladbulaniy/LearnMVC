@model LearnMVC.Models.GuestResponse
@{
    Layout = null;
}
<!DOCTYPE html>
<html>

<head>
    <meta name="viewport" content="width=device-width" />
    <title>Thanks</title>
</head>
<body>
    @{
        try
        {
            WebMail.SmtpServer = "sandboxef62b8fa27e1406da1a872d9d7a9cb0b.mailgun.org";
            WebMail.SmtpPort = 587;
            WebMail.EnableSsl = true;
            WebMail.UserName = "coollife@i.ua";
            WebMail.Password = "dertanz85";
            WebMail.From = "rsvps@yopmail.com";
            WebMail.Send("coollife@i.ua", "RSVP Notification",
            Model.Name + " is " + ((Model.WillAttend ?? false) ? "" : "not")
            + "attending");
        }
        catch (Exception)
        {
            @:<b>Sorry - we couldn't send the email to confirm your RSVP.</b>
        }
    }
    <div>
        <h1>Thank you, @Model.Name!</h1>
        @if (Model.WillAttend == true)
        {
            @:It's great that you're coming. The drinks are already in the fridge!
        }
        else
        {
            @:Sorry to hear that you can't make it, but thanks for letting us know.
        }
    </div>
</body>
</html>